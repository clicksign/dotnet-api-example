@model dynamic

@{
    ViewBag.Title = "title";
}

<h2>Teste do clicksign .net</h2>

<p>
    Para Iniciar, clique no botao abaixo. Irei fazer a seguinte rotina: </p>
    <ul>
    <li> Listar os Documentos existentes </li>
    <li> Fazer o upload de um novo Documento </li>
    <li> Listar novamente os documentos existentes (deve constar o novo documento criado) </i>
    </ul>:
<input type='button' id='iniciar' name='Iniciar' value='Iniciar' />
<div id='discussion' />


<!--Script references. -->
<!--The jQuery library is required and is referenced by default in _Layout.cshtml. -->
<script src="~/Scripts/jquery-2.1.3.js"></script>
<!--Reference the SignalR library. -->
<script src="~/Scripts/jquery.signalR-1.2.2.js"></script>
<!--Reference the autogenerated SignalR hub script. -->
<script src="~/signalr/hubs"></script>
<!--SignalR script to update the chat page and send messages.-->
<script>
    $(function () {
        // Reference the auto-generated proxy for the hub.
        var chat = $.connection.simpleHub;
        // Create a function that the hub can call back to display messages.
        chat.client.addMessage = function (message) {
            // Add the message to the page.
            $('#discussion').append('<li><strong>' + htmlEncode(name)
                + '</strong>: ' + htmlEncode(message) + '</li>');
        };

        // Start the connection.
        $.connection.hub.start(function () {
            $('#iniciar').click(function () {
                // Call the Send method on the hub.
                chat.server.startMessages();
            });
        });
    });
    // This optional function html-encodes messages for display in the page.
    function htmlEncode(value) {
        var encodedValue = $('<div />').text(value).html();
        return encodedValue;
    }
</script>
